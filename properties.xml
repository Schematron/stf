<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties version="1.0">
<comment>Properties for 'stf' system.</comment>
<!-- NOTE: Use ${stf.basedir.converted} for files located relative
     to the 'stf' system directory. -->

<!-- NOTE: It is an Ant convention that Ant-style property references
     in property values, e.g., "${stf.basedir.converted}", will
     resolve to that property's value, provided the property was
     previously declared either earlier in the build process or
     earlier in the current property file. -->

<!-- <oXygen/>-related properties. -->
<!-- Oxygen installation folder as file path. -->
<entry key="oxygenInstallDir">/usr/local/Oxygen XML Editor 13</entry>
<!-- When you have <oXygen/> installed, it's the easiest place to find
     the jars you need. -->
<entry key="calabash.jar">${oxygenInstallDir}/lib/xproc/calabash/calabash.jar</entry>
<entry key="saxon.jar">${oxygenInstallDir}/lib/saxon9ee.jar</entry>
<entry key="resolver.jar">${oxygenInstallDir}/lib/resolver.jar</entry>
<entry key="resolver.class">org.apache.xml.resolver.tools.CatalogResolver</entry>

<!-- Location of .jar for Saxon XSLT 2.0 (or later) processor -->
<entry key="saxon.jar.unix">/usr/share/java/saxonb.jar</entry>
<entry key="saxon.jar.mac">/usr/share/java/saxon9.jar</entry>
<entry key="saxon.jar.windows">/E:/saxon/saxon9he.jar</entry>


<!-- Location of SAX2 parser used by 'xsd' target. -->
<entry key="parser.jar">/usr/share/java/xercesImpl.jar</entry>

<!-- Schematron properties. -->
<!-- Top-level Schematron schema file. -->
<entry key="schematron">${system.basedir.converted}/schematron/testing.sch</entry>

<!-- Binding with which to run (not test) Schematron. -->
<entry key="schematron.binding">xslt2</entry>

<!-- Location of ISO Schematron XSLT implementation -->
<entry key="iso.schematron.dir">${stf.basedir.converted}/iso-schematron-${schematron.binding}</entry>
</properties>
